# # T2

## Task1

byte，short，int、long，float，double，char，boolean

---

- byte  占1字节  -128 ~ 127
- short  占2字节 -32768 ~ 32767
- int  占4字节 -2147483648 ~ 2147483647
- long  占8字节 $-2^{63}$  ~  $ 2^{63}-1 $

---

基本类型是存真实值，引用类型在堆内存开地址后存地址

---

隐式类型转换，char自动提升为 int

---

6. 13，++a=6，b++在运算时还是b=7
7. int 是32位有符号整数，最高位是符号位，如果总和超出 int 最大值高位进位会进到符号位，符号位变为 1，变为负数
8. float 使用二进制科学计数法存储小数。部分十进制小数转二进制是无限循环小数，存储时只能截断保存有限位数，丢失部分精度，计算时就会出现微小误差。

## Task2

### 2.1

```java
import java.util.Scanner;

public class App {
    static int cal(int year) {
        if (year % 400 == 0) return 0;
        if (year % 100 == 0) return 1;
        if (year % 4 == 0) return 2;
        return 3;
    }
    static boolean isLeapYear(int year) {
        return switch(cal(year)) {
            case 0 -> true;
            case 1 -> true;
            case 2 -> true;
            default -> false;
        };
    }
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);
        int year=sc.nextInt();
        if(isLeapYear(year)) {
            System.out.println("Yes");
        }
        else System.out.println("No");
    }
}

```



- switch适用于对同一个变量判断

- switch 支持的类型有限，在大量判断下switch会更快

### 2.2

```java
public class App {
    public static void main(String[] args) throws Exception {
        int n=5;
        for(int i=1;i<=n;i++) {
            for(int j=1;j<=n;j++) {
                if((i+j==(n>>1|1)+1)||(i+j==(n>>1|1)+n)||Math.abs(i-j)==(n>>1|1)-1) 
                    System.out.print("*");
                else System.out.print(" ");
            }
            System.out.println();
        }
    }
}

```

